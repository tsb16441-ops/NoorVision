{
    "version": 2,
    "installCommand": "pip install -r backend/requirements.txt",
    "builds": [
        {
            "src": "backend/server.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/server.py"
        }
    ]
}